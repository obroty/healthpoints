http://wopr.szczecin.pl/zachodniopomorska-siec-defibrylatorow-aed/

otwarte kwestie:
czy zduplikowany adres oznacza więcej kolejny defibrylator pod podanym adresem?

import sys
import math

# Auto-generated code below aims at helping you parse
# the standard input according to the problem statement.

def calc_distance(lonA, latA, lonB, latB):
    x = (lonB-lonA)*math.cos((latA+latB/2))
    y = (latB-latA)
    d = math.sqrt(x**2 + y**2)*6371
    return d

lon = input().replace(',','.')
lat = input().replace(',','.')
n = int(input())
defib_ar = []
distances = []
for i in range(n):
    defib = input().split(';')
    defib_ar.append(defib)
    
for defib in defib_ar:
    d_lon = defib[4].replace(',','.')
    d_lat = defib[5].replace(',','.')
    distances.append(calc_distance(float(lon), float(lat), float(d_lon), float(d_lat)))

min_dist = float(distances[0])
closest_spot_id =  0
for i in range(1, len(distances)):
    dist = float(distances[i])
    if dist < min_dist:
        min_dist = dist
        closest_spot_id = i

print(defib_ar[closest_spot_id][1])

